export default function ResidentsPage() {
  return (
    <section className="rounded-2xl border border-[var(--border)] bg-[var(--card)] p-6 shadow-sm">
      <h1 className="text-xl font-semibold text-[var(--text)]">Residents</h1>
      <p className="mt-2 text-sm text-[var(--muted)]">
        Resident registry UI placeholder. Connect data sources and table modules in the next phase.
      </p>
    </section>
  );
}
